# Shell scripts must keep LF line endings regardless of the checkout OS --
# a CRLF shebang line breaks `sh` inside Linux containers ("/bin/sh^M: bad
# interpreter").
*.sh text eol=lf
Dockerfile text eol=lf
